const readline=require("readline")
.createInterface({
    input:process.stdin,
    output:process.stdout,
});
readline.question("Enter the size of array: ",(a)=>{
    readline.question("Enter value: ",(b)=>{
        
         let arr=new Array();
        for(let i=0;i<arr.length;i++){
        
        }

        readline.close();
    });
});